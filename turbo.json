{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".wrangler/**"]
    },
    // apps/web depends on @cfmail/worker only for types (Hono RPC AppType,
    // imported straight from worker sources) — no build artifacts are needed,
    // and inheriting ^build would close a cycle with the worker's asset embed.
    "@cfmail/web#build": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "@cfmail/web#typecheck": {
      "dependsOn": [],
      "outputs": []
    },
    "@cfmail/worker#build": {
      "dependsOn": ["^build", "@cfmail/web#build"],
      "outputs": ["dist/**", ".wrangler/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
